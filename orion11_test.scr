Сценарий
    Переменные
        Перем РабочееМесто1;
        Перем РабочиеМеста1;

        Перем Компьютер1;
        Перем Компьютеры1;

        Перем ГруппыДоступа1;

        Перем ЗоныДоступа1;
        Перем ЗонаДОступа1;

        Перем Сотрудники //Проверяем, если пароль сотрудника принадлежит уровню доступа из первого цикла уровней доступа

    ЗоныДоступа1 = РабочееМесто1.ЗоныДоступа();
    ЗонаДоступа1 = ЗоныДоступа1.ПолучитьПоИд(2); //Зона доступа Парковка (например)

    Сотрудники1 = РабочееМесто1.Сотрудники();
    КолвоСотрудников = Сотрудники1.Количество(); //Узнаем кол-во сотрудников

    //Перебираем все уровни начиная с первого
    Для СчетчикГрупп = 1 По КолвоГрупп Цикл
        //Проверяем, если в зоне доступа Парковка кол-во сотрудников больше или равно 1
        Если ЗонаДоступа1.КоличествоСотрудниковПоУровню(СчетчикГрупп) >= 1 Тогда
            //Запускаем цикл по кол-ву сотрудников, начиная с первого
            Для СчетчикСотрудников = 1 По КолвоСотрудников Цикл
               //Получаем сотрудника
               Сотрудник1 = Сотрудники1.ПолучитьПоИд(СчетчикСотрудников);
               //Проверяем, входит-ли сотрудник в зону Внешний мир
               Если Сотрудник1.ЗонаДоступа() == 1 Тогда
                    // Получаем пароль
                    Пароли1 = Сотрудник1.Пароли();
                    //текущего сотрудника
                    Пароль1 = Пароли1.Первый();
                    //Проверяем, если пароль сотрудника принадлежит уровню доступа из первого цикла
                    Если Пароль1.ГруппаДоступа() == СчетчикГрупп Тогда
                         Сотрудник1.Заблокировать(); //Блокируем карты данного сотрудника
                    КонецЕсли;
               КонецЕсли;
           КонецЦикла;
      КонецЕсли;
    КонецЦикла;

    КонецСценария
